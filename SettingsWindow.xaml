<Window x:Class="VoiceTyping.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Voice Typing Settings"
        Width="400" Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1e1e2e"
        WindowStyle="None"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Window.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2a2a3e"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#ffffff"/>
        </Style>
        
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#2a2a3e"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderBrush" Value="#3a3a4e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#ffffff"/>
        </Style>
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#667eea"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#764ba2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#3a3a4e"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4a4a5e"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="12" Background="#1e1e2e" BorderBrush="#3a3a4e" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="⚙️" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Foreground="#ffffff" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- API Key -->
            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                <TextBlock Text="OpenAI API Key" Foreground="#888888" FontSize="12" Margin="0,0,0,6"/>
                <PasswordBox x:Name="ApiKeyBox" Style="{StaticResource ModernPasswordBox}" PasswordChar="●"/>
            </StackPanel>
            
            <!-- Language -->
            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                <TextBlock Text="Language (optional, e.g., 'en', 'th', 'ja')" Foreground="#888888" FontSize="12" Margin="0,0,0,6"/>
                <TextBox x:Name="LanguageBox" Style="{StaticResource ModernTextBox}" />
            </StackPanel>
            
            <!-- Hotkey Info -->
            <Border Grid.Row="3" Background="#2a2a3e" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🎤" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Foreground="#888888" FontSize="13" VerticalAlignment="Center">
                        <Run Text="Press"/><Run Text=" F8 " Foreground="#667eea" FontWeight="Bold"/><Run Text="to toggle recording"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CancelButton_Click" Margin="0,0,12,0"/>
                <Button Content="Save" Style="{StaticResource ModernButton}" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
